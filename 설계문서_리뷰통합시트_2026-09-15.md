# 설계문서: 리뷰 통합 시트

- 작성일: 2026-09-15
- 파일럿 병원: 강남제이스타의원
- 폴더: `01_컨설팅/05_CS/팀 프로젝트/리뷰 통합 시트/`

## 1. 목적

병원별 실시간(에 가까운) 리뷰(구글/네이버/카카오) 통합 모니터링 및 CS 개선 데이터
시각화 도구. 컨설턴트와 병원 관계자 모두 링크 하나로 접속해 확인할 수 있어야 한다.

**참고**: 유사한 목적의 "리뷰 컨트롤타워/VOC 통합" 프로젝트가 과거(세마그룹 공용
템플릿 시절)에 존재했으나, 현재 이 작업공간(CS팀 이전 사본)에는 해당 폴더가
없다 — 이전 과정에서 누락된 것으로 보인다. 사용자 확인 결과 이번 건은 **완전히
별개의 새 프로젝트**로 진행하며, 과거 프로젝트의 수집기(collectors) 로직만
참고·재사용한다.

## 2. 핵심 제약과 그에 따른 아키텍처 선택

- **담당자 PC가 24시간 켜져 있을 수 없음** → 매일 13시 자동 수집은 PC가 아니라
  **GitHub Actions**(무료 클라우드 자동실행)에서 돈다.
- **서버 상시 운영·호스팅 비용 없이 외부(병원 관계자) 공개** →
  **GitHub Pages**(무료 정적 호스팅)로 프론트를 배포한다.
- **AI 판단(감성분류) 작업은 원래 Claude Code 세션이 담당하는 것이 원칙**(CLAUDE.md)
  이지만, GitHub Actions는 사람도 세션도 없는 완전 무인 환경이라 그대로 쓸 수 없음.
  → **규칙 기반 분류로 대체**(아래 5절). 유료 Anthropic API 도입은 사용자가
  선택하지 않음(비승인 상태 유지).
- **실시간 부정 리뷰 웹푸시 알림은 불필요**(사용자 확정) → 웹푸시/상시 서버(Flask)
  구조를 도입하지 않는다.

결과적으로 이 프로젝트는 **완전 정적 프런트 + 무료 자동실행 파이프라인 +
스프레드시트 저장소** 구조이며, 운영 중에는 사람/Claude 세션 개입이 필요 없다.

## 3. 아키텍처 개요

```
[GitHub Actions, 매일 04:00 UTC(=13:00 KST)]
   1) collectors/ 실행 (네이버 GraphQL 직접호출, 카카오/구글 Playwright 렌더링)
   2) Google Sheets에서 기존 리뷰 읽어 (channel, content) 기준 dedup
   3) 신규 리뷰만 규칙 기반 분류 엔진 통과 (5절)
   4) Google Sheets에 신규 행 append (서비스 계정 API 인증)

[GitHub Pages, 정적 사이트]
   병원 관계자가 링크 접속
   → 브라우저 JS가 Google Sheets 데이터를 fetch
   → 클라이언트에서 기간별 집계·필터링·렌더링 (재배포 불필요, 항상 최신)
```

## 4. 구성요소

| 구성요소 | 역할 | 비고 |
|---|---|---|
| `collectors/naver.py`, `kakao.py`, `google.py` | 채널별 원본 리뷰 수집 | 기존 리뷰 컨트롤타워 로직 이식. GitHub Actions Ubuntu 러너에서 Playwright 브라우저 설치 스텝 필요 |
| `classifier/rule_based.py` | 규칙 기반 감성분류·키워드 추출 | 5절 참고 |
| `sheets_writer.py` | Google Sheets API로 신규 리뷰 append | 서비스 계정 인증, dedup 책임 포함 |
| `.github/workflows/daily_collect.yml` | 매일 13:00 KST 트리거 + 수동 실행(workflow_dispatch) | |
| `index.html` (GitHub Pages) | 대시보드 프론트 | 6절 참고 |
| `add_hospital.py` (추후) | 신규 병원 온보딩 CLI | 기존 프로젝트 패턴 참고 (주소=지번주소 필수 등) |

## 5. 규칙 기반 분류 엔진

정확도를 최대한 확보하기 위한 조합:

1. **별점 우선판정**: 카카오·구글처럼 별점이 있는 리뷰는 별점으로 즉시 확정
   (≤2점 부정, ≥4점 긍정, 3점 중립). 네이버는 수집 구조상 별점이 항상 null이라
   대상 아님.
2. **KNU 한국어 감성사전 기반**: 공개·무료 오픈소스 사전을 기본 어휘로 사용.
3. **병원 도메인 특화 키워드 보강**: "친절/불친절", "대기시간 김/짧음",
   "설명 잘해줌/무성의", "깨끗함/비쌈" 등 병원 리뷰 특유 표현을 추가 사전으로 얹음.
   (이 사전 구축은 최초 1회성 설계 작업 — PC 켜진 상태에서 진행, 매일 반복되는
   판단이 아니므로 무인 운영 원칙과 상충하지 않음.)
4. **부정어 처리**: "안 친절", "친절하지 않다"처럼 부정어가 긍정 단어를 뒤집는
   경우를 규칙으로 반전 처리.
5. **애매한 경계는 정직하게 "미확정" 표시**: 점수가 임계값 근처면 추정하지 않고
   `confirmed: false`로 저장. 담당자가 원할 때 Claude Code 세션에서 재검토할 수
   있으나, 필수 단계는 아님(시스템은 미확정 상태로도 정상 동작).
6. **키워드 TOP5**: 형태소 분석(예: KoNLPy) 기반 명사 빈도 추출. AI 판단이 아닌
   결정적 처리이므로 GitHub Actions에서 문제없이 실행 가능.

## 6. 프론트엔드 (GitHub Pages)

- **디자인 시스템**: 과거 세마그룹 공용 템플릿 시절 만들었던 "리뷰 컨트롤타워"
  프로젝트(`VOC통합대시보드_멀티병원_v1.html`, 현재는 구글 드라이브에만 원본이
  남아있고 이 작업공간에는 없음)의 시각 디자인을 그대로 이식하기로 확정
  (2026-09-15, 사용자 확정). 네이비 사이드바(`--navy-900:#0F1E42` 등)+콘텐츠
  2단 구조, 폰트는 `IBM Plex Sans KR`(display)+`Noto Sans KR`(body), 채널색은
  구글=`--chart-1`(blue) / 카카오=`--chart-2`(orange) / 네이버=`--chart-3`(aqua),
  상태색은 `--good`/`--critical`/`--warning`. 사이드바 메뉴는 원본 5개 중
  "알림 설정"만 제외(본 프로젝트는 실시간 알림 없음으로 확정 — 4절 참고),
  나머지(대시보드 홈/키워드&추이/경쟁·지역 지도/통합 리뷰 관리)는 동일.
- **병원명 비식별화(하드코딩 금지)**: 헤더·타이틀의 병원명은 `?hospital=`
  쿼리파라미터(또는 향후 도입할 토큰)로 동적 표시. 파라미터가 없으면
  기본값 "지점별 VOC 통합 대시보드"를 표시. 토큰 파라미터 존재 여부로
  "토큰 인증됨"/"샘플 모드" 배지도 함께 전환.
- **상단 통계 카드(KPI)**: 총 리뷰 수, 평균 평점(별점 제공 리뷰만 집계),
  긍정 비율, 부정 리뷰(별점 2점 이하) 건수 — 전부 **1/3/6개월 토글에
  연동**(기본값 3개월).
- **플랫폼별 리뷰 건수 및 비중 카드**: 채널 배지(N/K/G)+막대+건수·비중,
  클릭 시 통합 리뷰 목록으로 이동하며 해당 채널로 필터링.
- **키워드 & 추이 섹션**: 긍정 주요 키워드 TOP5 / 부정 주요 키워드 TOP5를
  별도 카드로 분리 표시(원본 디자인 방식), 채널별 비중은 도넛 차트.
- **월별 추이 그래프**: 긍정 vs 부정 추이(토글과 무관하게 **항상 최근 6개월
  고정**).
- **드릴다운**: 플랫폼별 집계 항목, 부정 리뷰 KPI 카드 클릭 시 통합 리뷰
  목록 섹션으로 이동하며 해당 조건(채널/부정만)으로 필터링. 리뷰 목록에는
  채널 탭 필터, 텍스트 검색, "부정 리뷰만" 토글을 제공하고 부정 리뷰는
  카드 왼쪽 테두리 강조로 하이라이트.
- **경쟁병원·지역 비교 지도**: 원본과 동일하게 **실제 Leaflet + OpenStreetMap**
  (무료, 등록 불필요) 코드를 그대로 사용. 커스텀 마커는 말풍선형
  (`★평점 | 리뷰수건`, 자사는 "내 병원" 네이비 마커), 부정 급증은 ⚠️,
  긍정 상승은 🟢 이모지 배지로 표시(원본 방식 그대로).
  - 행정동 기준으로 경쟁병원 자동 매칭(도로명주소가 아닌 지번주소에서
    행정동 추출 — 기존 프로젝트에서 확인된 필수 조건).
  - **VOC 급증 판정**: 직전 동일 기간 대비 증가율 기준(긍정/부정 각각 별도
    판정). 기본 임계값은 **해당 기간 리뷰 3건 이상 AND 직전 동일기간 대비
    50% 이상 증가**(표본이 너무 적을 때 사소한 변동을 급증으로 오판하지
    않기 위함). 실제 데이터로 운영해보며 조정 가능.
  - **주의**: Claude Artifact 미리보기 환경은 CSP상 지도 타일 이미지 요청이
    차단되어 마커만 보이고 타일은 회색으로 빈다(마커·데이터 렌더링 자체는
    정상, 타일 로드 실패만 원본의 `tileerror` 핸들러로 안내). GitHub Pages
    실배포 환경에는 이 제약이 없다.
- **데이터 없음 표기**: 채널 수집 실패, 미확정 분류 등은 추정치로 채우지 않고
  명시적으로 "데이터 없음"/"미확정" 표기.

## 7. 오류 처리

- 채널별 수집은 독립적으로 실패 처리 — 한 채널이 실패해도 나머지는 정상 집계,
  실패 채널만 "수집 실패" 표기.
- 네이버 봇 차단 감지 시 기존과 동일하게 명시적 에러 발생 → 사람이 확인 후
  시트에 직접 채워 넣는 수동 경로로 대체.
- Google Sheets API 쓰기 실패(rate limit 등)는 재시도 후, 그래도 실패하면
  다음 회차 수집 때 자동 재시도(누적 dedup 구조라 데이터 유실 없음).

## 8. 테스트 계획

- 각 신규/이식 모듈 독립 테스트: 수집 파싱, dedup, 규칙기반 분류, 행정동 추출,
  VOC 급증 계산.
- GitHub Actions 워크플로우를 `workflow_dispatch`(수동 트리거)로 먼저 실행해
  실제 시트에 데이터가 정확히 반영되는지 확인.
- 브라우저 실측: 카드·그래프·지도·드릴다운·기간버튼 전부 클릭, **모달을 열어둔
  채 스크롤까지** 확인(과거 유사 프로젝트에서 지도 z-index가 모달을 뚫고 올라오는
  버그가 정적 스크린샷만으로는 안 보였던 전례 있음).

## 9. 사전 준비물 (사용자 진행, 단계별 안내 예정)

1. GitHub 저장소 신규 생성 (사용자 보유 계정 사용)
2. GitHub Pages 활성화
3. Google Sheet 신규 생성 + Google Cloud 서비스 계정 발급 + 시트에 해당
   서비스 계정 공유 권한 부여
4. GitHub Actions Secrets에 서비스 계정 인증정보 등록

## 10. 확정된 결정 요약 (변경 시 주의)

- 완전히 새 프로젝트, 기존 "리뷰 컨트롤타워" 폴더와 무관(단, collectors 로직만 참고)
- 실시간 알림 없음, 매일 13시 자동 집계만
- 분류는 규칙 기반(별점우선+KNU사전+병원특화키워드+부정어처리+미확정표시),
  유료 API 미도입
- 지도는 무료 Leaflet/OSM, 실제 네이버 지도 API 미사용
- 기간 토글(1/3/6개월)은 상단 통계 카드 전체에 적용, 월별 추이는 6개월 고정
- VOC 급증 기준은 직전 동일기간 대비 증가율
- 데이터 저장소는 Google Sheets, 프론트는 GitHub Pages
